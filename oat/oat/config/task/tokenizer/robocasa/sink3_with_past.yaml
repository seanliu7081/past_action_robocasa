name: sink3_with_past

task_name: sink3

shape_meta: &shape_meta
  action:
    shape: [12]

dataset:
  _target_: oat.dataset.zarr_dataset_with_past.ZarrDatasetWithPastAction
  zarr_path: data/robocasa/sink3_N${training.num_demo}.zarr
  obs_keys: []
  action_key: action
  n_obs_steps: 0
  n_action_steps: ${horizon}
  past_n: ${past_n}
  seed: ${seed}
  val_ratio: 0.1
